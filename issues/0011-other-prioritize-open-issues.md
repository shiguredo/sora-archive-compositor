# 残 open issue を軽いものから順に片付ける

- Created: 2026-08-17
- Completed: {YYYY-MM-DD}
- Branch: feature/update-open-issue-work-order
- Polished: {YYYY-MM-DD}

## 目的

残っている open issue を、影響範囲の小さいものから順に片付ける。作業ブランチとレビューの単位を壊さないため、実装は常に子 issue 側で行い、本 issue は推奨順の台帳として使う。

順不同で着手すると、公開前の掃除とパイプライン横断バグが同時に走り、レビューと revert の単位が壊れる。

## 現状

`issues/` 直下の open は次の 7 件（本 issue 自身を除く）。pending は無い。open のうち `Polished:` が入っているのは 0006。0007 は対応不要、0008 / 0009 は実装済みで closed にした。軽い掃除枠は一通り消化し、残りは後回し・保留のみ。

- 保留: 0005
- 後回し: 0001, 0002, 0003, 0004, 0006, 0010

子 issue の本文は本台帳では要約しない。着手時は必ずその issue を読む。

今すぐ着手する子 issue は無い。再開時は「後回しを戻す順」を参照する。

## 設計方針

- 1 issue = 1 ブランチ。本 issue のブランチでは子 issue のコード変更をしない
- 着手前に対象 issue を polish する。未 polish のまま実装しない
- 依存があるものは先に依存元を閉じる
- リリース関連（Dockerfile・canary・リリース workflow・crates.io）は公開直前まで後回しにする
- 0010 の crates.io 公開方針は公開直前に決める。今は着手しない

### 推奨順

番号は推奨着手順であり、issue 番号ではない。

#### 後回し (ユーザー判断)

再開するときは上から順を基本とする。0002 はドキュメント整理 (0004) より前にやる。

1. 0002 hisui 2025.3.2 との録画合成性能を比較する — いったん後回し。**0004 より前に実施する**
2. 0004 migration_from_hisui_2025_3_2.md を整理する — 修正方法が非自明なため慎重に進める。いったん後回し
3. 0001 Dockerfile と canary.py を移植する — リリース関連のため後回し
4. 0003 リリース用 GitHub Actions ワークフローを追加する — リリース関連。Docker ジョブは 0001 依存のため同様に後回し
5. 0010 crates.io 公開方針を決定し Cargo.toml を整備する — 公開の有無は公開直前に決める。今は着手しない
6. 0006 VideoToolbox / NVCODEC のエラーが握り潰される — 握り潰しはバグだが、局所の `Result` 化では足りずコールバックキューの fail-fast 契約まで含む。方針は 0006 本文に固定した

#### 保留

- 0005 tune 結果に基づくデフォルトパラメータの決定 — 結果が溜まるまで保留。溜まったら pending 扱いを解き着手する

### 依存関係

- 0001 → 0003 の Docker イメージジョブ（どちらも後回し中）
- tune 実行ワークフローの結果 → 0005
- 0002 を 0004 より前に実施する（ユーザー判断）

### 本 issue でやらないこと

- 子 issue の実装・polish・closed への移動
- 推奨順の強制。依存を壊さない範囲で前後してよい
- 新しいバグやリファクタの起票 (見つかったら別 issue にする)

## 完了条件

- 台帳に挙げた子 issue がすべて closed または pending になっている
- 0005 のように結果待ちのものは pending へ移し、本台帳上は消化済みとして扱う
- 推奨順を更新する必要が出た場合は、本 issue の「推奨順」を書き換えてから子 issue に着手する
